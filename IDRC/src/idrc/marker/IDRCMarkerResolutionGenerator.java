package idrc.marker;

/*responsible for calculating the possible fixes: it reads the marker, it can check the related files, etc.,
and creates marker resolution instances. 
These resolution instances basically process the erroneous files, and hopefully fix the original problem.*/

public class IDRCMarkerResolutionGenerator{

}
